# Server-side protection secrets
# Copy this file to .env.local and set your own secure values

# JWT Secret for API token generation (use a strong random string)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-256-bits

# Crypto Secret for data encryption (use a strong random string)
CRYPTO_SECRET=your-crypto-secret-key-for-data-encryption-change-this-too

# Example generation commands:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# openssl rand -hex 32